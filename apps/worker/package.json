{
  "name": "@conversational-ai/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "echo \"Worker scaffold — Phase 2+\""
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
